import { GhPagesLinkPipe } from './gh-pages-link.pipe';

describe('GhPagesLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new GhPagesLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
